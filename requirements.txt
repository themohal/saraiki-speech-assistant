# Pin Streamlit Cloud to Python 3.12 (App settings -> Python version).
# On 3.14 there are no numba/torch wheels and the install fails.
streamlit>=1.40
google-generativeai>=0.8.5
transformers>=5.3.0
torch>=2.4
torchaudio>=2.4
omnivoice>=0.2.1
soundfile>=0.12.1
numpy>=1.26
accelerate>=0.30

# librosa is deliberately NOT listed. The microphone sends 16 kHz mono WAV,
# which soundfile reads directly. librosa is only needed to decode compressed
# uploads (WebM/MP3/M4A) and it drags in numba/llvmlite, which cannot build on
# newer Python versions. Install it yourself if you need those formats:
#   pip install librosa
