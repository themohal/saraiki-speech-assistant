# Streamlit Cloud: set the Python version to 3.12 (see runtime.txt).
#
# numpy is capped below 2.5 on purpose. omnivoice pulls in librosa -> numba,
# and numba 0.66 requires numpy<2.5. Without this cap the resolver walks numba
# backwards to 0.53.1, whose llvmlite==0.36.0 has no wheels and refuses to
# build on any Python above 3.9 -- that is the "Cannot install on Python
# version 3.12" build failure.
numpy>=1.26,<2.5

streamlit>=1.40
google-genai>=1.0
transformers>=5.3.0
torch>=2.4
torchaudio>=2.4
omnivoice>=0.2.1
soundfile>=0.12.1
accelerate>=0.30
